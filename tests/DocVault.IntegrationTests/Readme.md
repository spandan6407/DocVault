This test project contains integration and functional test cases for the DocVault APIs. It uses xUnit and Microsoft.AspNetCore.Mvc.Testing to host the APIs in-memory for tests. The tests are intended as templates and should be adapted to the local environment where RabbitMQ and SQL Server may be required. Ensure to set environment variables (RABBITMQ__CONNECTIONSTRING, ASPNETCORE_ENVIRONMENT) before running tests.
